Binary RPMs at https://ktdreyer.fedorapeople.org/macbook/

To refresh the DNF cache for this repo's data:

 dnf --disablerepo='*' --enablerepo=ktdreyer-macbook --refresh

To install the kmods for your running kernel:

 dnf install kmod-wl-$(uname -r) kmod-facetimehd-$(uname -r)
